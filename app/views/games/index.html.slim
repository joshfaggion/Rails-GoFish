h1 Welcome to Go Fish

= link_to 'Create Game', new_game_url

h2 Pending Games:
- if @pending_games.blank?
  p No Pending Games.
- else
  - @pending_games.each do |game|
    - if game.pending?
      p = link_to "Join Game #{game.id} - waiting for players", games_join_path(game.id), method: :post
    - else
      p = "Game #{game.id} is in progress"
